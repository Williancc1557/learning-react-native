import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: '100%',
    backgroundColor: '#3af47e',
    borderRadius: 30,
  },

  labelStyle: {
    textTransform: 'none',
    paddingVertical: 5,
  },
});
